machine:
  node:
    version: 5.7.1
dependencies:
  pre:
    - npm install -g tslint
    - npm install -g typings
    - npm install -g jasmine
    - typings install
test:
  override:
    - npm run test
